{
  "name": "a3-data",
  "version": "1.0.0",
  "description": "Design Decisions: I have chose to rather then have the order summary in a another column beside the menu to have its own section at the top of the page where a dropdown list is shown. This was learned from w3schools.com.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pug": "^3.0.2"
  }
}
